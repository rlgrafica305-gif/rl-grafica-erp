{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "php artisan storage:link --force 2>/dev/null; php artisan serve --host=0.0.0.0 --port=${PORT:-8080} & sleep 5 && php artisan migrate --force --no-interaction && php artisan db:seed --force --no-interaction; wait"
  }
}
